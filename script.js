const year=document.getElementById('year');const header=document.querySelector('.header');const menu=document.querySelector('.menu');if(year)year.textContent=new Date().getFullYear();if(menu&&header){menu.addEventListener('click',()=>{const open=header.classList.toggle('open');menu.setAttribute('aria-expanded',String(open));menu.setAttribute('aria-label',open?'Close menu':'Open menu')})}document.querySelectorAll('nav a').forEach(link=>link.addEventListener('click',()=>{header?.classList.remove('open');menu?.setAttribute('aria-expanded','false')}));
